# 隐私计算对接流程

- 对接参考隐私计算平台API接口文档，AGENTS.md仅代表我对系统的一些理解，可能会有错误。
- 记得check数据库，密码后端里有。
- 后端代码实现放在pcp文件夹中，可以这么拆pcp/index.js, pcp/he.js, ...

## HE对接
- 对于前端，我们要改卖家和买家的资产交付界面。卖家资产交付界面我想界面的条目是这样，交易ID，交付状态，交付方法(下拉框)，数字合约查看按钮，交付按钮；买方这边的条目要有，交易ID，交付状态，交付方法，查看合约按钮，下载结果按钮，可能还有其他按钮。对于HE而言，卖家第一次点交付会显示需要买方上传同态加密的公钥(因为数据库里差不多对应的公钥，可以考虑这样实现)，与此同时买方那边也会新增条目，会多一个上传公钥的按钮(点击的话会生成与合约id绑定的两套私公钥，公钥要存数据库，可以创建个新表，专为HE所用，表里面有合约id，Paillier公钥，ElGamal公钥)，所以你需要在前端实现这两套算法，这里我们只加密csv文件，计算的话由后端来做。买方上传后，卖方点击交付，会弹窗供用户填写，里面根据接口文档，有两个符合格式的数字csv文件，加密算法的选择，加密算法对应操作符的选择。交付成功后，买方可以通过下载按钮可以进行下载。

## Database Access

To inspect MySQL data, use the following connection:

```bash
mysql -u root -p -e "SELECT ..."
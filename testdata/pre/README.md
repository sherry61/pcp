# PRE testdata

- `source_cipher_zip.zip`: 源密文 zip 占位样例。
- `target_public.pem`: 目标接收方公钥占位样例。
- `reencryption_key.example.json`: 仅说明用途，不是可直接用于 PCC 标准 PRE attempt 的真实 rk。

注意：PRE 当前仍未切到 PCC 标准 attempt 材料流，这里先提供目录结构与占位样例，不冒充真实可运行密码学数据。

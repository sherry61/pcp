# HE testdata

- `seller_a.csv`, `seller_b.csv`: HE CSV 样例，表头为 `cipher`。
- `paillier_public_key.json`: 对应 PCC 文档里的 public_key 样例。

注意：当前仓库里的 HE 页面/后端还没有完全切到 PCC 新 attempt seller_files 模型，这组数据主要用于后续重构和接口联调。
